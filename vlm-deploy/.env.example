# =============================================================================
# .env.example — Environment variables for Qwen3.6-35b-a3b vLLM deployment
# =============================================================================
# Copy this file to .env and edit as needed:
#   cp .env.example .env
#
# All values have sensible defaults; only override what you need.
# =============================================================================

# ---------------------------------------------------------------------------
# Docker image name/tag (used by compose.yaml)
# ---------------------------------------------------------------------------
IMAGE_NAME=vlm-qwen36-35b-a3b
IMAGE_TAG=latest

# ---------------------------------------------------------------------------
# Model configuration
# ---------------------------------------------------------------------------
# The model is baked into the image by scripts/build.sh from:
#   vlm-deploy/models/Qwen3.6-35B-A3B-FP8
MODEL_PATH=/models/Qwen3.6-35B-A3B
SERVED_MODEL_NAME=qwen3.6-35b-a3b

# ---------------------------------------------------------------------------
# Server configuration
# ---------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000

# ---------------------------------------------------------------------------
# GPU / memory settings
# ---------------------------------------------------------------------------
GPU_MEMORY_UTILIZATION=0.60
TENSOR_PARALLEL_SIZE=1

# ---------------------------------------------------------------------------
# Model length / batching
# ---------------------------------------------------------------------------
MAX_MODEL_LEN=4096
MAX_NUM_SEQS=8
PERFORMANCE_MODE=interactivity
ENABLE_PREFIX_CACHING=true
LIMIT_MM_PER_PROMPT={"image":1}
DEFAULT_CHAT_TEMPLATE_KWARGS={"enable_thinking":false}

# ---------------------------------------------------------------------------
# MTP speculative decoding (true/false)
# Set to false to disable MTP if it causes startup issues.
# ---------------------------------------------------------------------------
ENABLE_MTP=true
MTP_SPECULATIVE_TOKENS=1

# ---------------------------------------------------------------------------
# Hugging Face token (for build only — not needed at runtime)
# Set this before running scripts/build.sh:
#   export HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxx
# Optional:
#   export MODEL_REPO=Qwen/Qwen3.6-35B-A3B-FP8
#   export MODEL_DIR_NAME=Qwen3.6-35B-A3B-FP8
# ---------------------------------------------------------------------------
# HF_TOKEN=
